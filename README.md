# dd2610-project

Don’t Diffuse, Just Flow – One-Step Generative Modeling

## Run training 

Set the parameters in the config.toml file and simply run the following command:

```bash
uv run main.py
```

## Turn up tensorboard

tensorboard --logdir=/workspace/runs --host=0.0.0.0 --port=6006

Authors: Nikita Suprun, Fredrik Nguyen, Sirui Li
